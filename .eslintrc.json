{
  "extends": "next/core-web-vitals",
  "rules": {
    "no-empty": ["error", { "allowEmptyCatch": false }],
    "no-restricted-syntax": [
      "error",
      {
        "selector": "CallExpression[callee.object.name='Math'][callee.property.name='random']",
        "message": "REGRA 00: Math.random() e terminantemente proibido. Serie historica e motores quantitativos devem ser reproduziveis e deterministicos."
      }
    ]
  },
  "overrides": [
    {
      "files": ["src/lib/domain/**/*.ts", "src/lib/services/**/*.ts"],
      "rules": {
        "no-restricted-syntax": [
          "error",
          {
            "selector": "CallExpression[callee.object.name='Math'][callee.property.name='random']",
            "message": "REGRA 00: Math.random() e terminantemente proibido. Serie historica e motores quantitativos devem ser reproduziveis e deterministicos."
          },
          {
            "selector": "LogicalExpression[operator='||'][right.type='Literal'][right.raw=/^[0-9]/]",
            "message": "REGRA 00: Fallback magico com || <numero literal> e proibido em domain/services."
          }
        ]
      }
    },
    {
      "files": ["src/components/**/*.tsx", "src/components/**/*.jsx"],
      "excludedFiles": ["src/components/shared/DataValue.tsx"],
      "parser": "@typescript-eslint/parser",
      "parserOptions": {
        "project": "./tsconfig.json"
      },
      "plugins": ["local-rules"],
      "rules": {
        "local-rules/no-raw-numbers-in-jsx": "error",
        "no-restricted-syntax": [
          "error",
          {
            "selector": "CallExpression[callee.property.name='toFixed']",
            "message": "REGRA 00: Proibido .toFixed() em componentes de UI, mesmo fora do JSX (ex: pre-calcular numa variavel e so interpolar a variavel nao escapa a regra). Use o componente estrutural <DataValue format=\"...\" /> com proveniencia e fonte obrigatorias."
          }
        ]
      }
    }
  ]
}
